apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "balkanid.fullname" . }}-blobs
spec:
  accessModes: [{{ .Values.storage.accessMode }}]
  resources:
    requests: { storage: {{ .Values.storage.size }} }

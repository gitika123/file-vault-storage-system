# Issue 022 - Kubernetes deployment artifacts

**Status:** CLOSED

The `k8s/` folder contains namespace, API Deployment/Service, health probes, environment Secret reference, and blob PVC. The image name is an explicit release placeholder and must be replaced before deployment.
